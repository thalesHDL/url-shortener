import { IEncoderService } from "../service/encoder.service";
import { UrlShortenerService } from "../service/url-shortener.service";
import { UrlShortenerController } from "./url-shortener.controller"

describe("UrlShortenerController", () => {
    let controller: UrlShortenerController;
    let service: UrlShortenerService;
    let encoderService: IEncoderService;

    // TODO: descobrir um jeito de mockar o repository
})
